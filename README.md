# Vinson Portfolio

A minimal personal portfolio for Vinson, built with React and Vite.

## Local development

```bash
npm install
npm run dev
```

## Production build

```bash
npm ci
npm run build
```

The site source lives on `main`. The production build is published from the `gh-pages` branch.
